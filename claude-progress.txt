# Progress Log
# プロジェクト: kb-mcp
# 開始日: 2026-04-18

## 2026-04-18
- harness-kit 初期化完了
- features.json 作成 (22 件の機能を登録: core 6 passing / upgrade 14 failing / quality 2 failing)
- 本セッション内で下記 fix を先行実施:
  - fix(db): sqlite-vec knn クエリに `k = ?` 制約を追加 (1e613fd)
  - chore(gitignore): Claude Code ローカル設定を無視 (d39dfa5)
- feature 8 実装完了 (status: passing, commit ccc9c8d)
  - Plan エージェントで仕様化 → 5 ステップで実装
  - 追加: Embedder::model_id, Database::{read,write,verify}_embedding_meta
  - init() で index_meta を vec_chunks より先に CREATE (feature 7 下地)
  - verify 呼び出し: main.rs::Commands::Index と server.rs::run_server
  - 新規テスト 5 件 + 既存 6 件 すべてパス (合計 11 / 22 全体)
  - 実 DB (ai_organization/.kb-mcp.db) で migration → idempotent 動作確認
- feature 7 実装完了 (status: passing)
  - モデル調査: fastembed-rs 対応の多言語モデルを網羅比較
  - 選定: BGE-M3 (1024 dim, MIT, プレフィックス不要)。次点 mE5-large
  - 不採用: Ruri-large / jina-v3 は fastembed-rs enum 未登録のため
  - 6 ステップ実装:
    1. ModelChoice enum (clap::ValueEnum derive)
    2. Embedder::with_model (new() は委譲で後方互換)
    3. vec_chunks 遅延作成 + ensure/current_vec_dim + parse_dim_from_create_sql
    4. recreate_vec_chunks / reset_for_model (--force 用の破壊的再初期化)
    5. CLI --model flag (Serve/Index に配線、run_server シグネチャ変更)
    6. README.md / CLAUDE.md に --model 使用例追記、BGE-M3 ignored テスト
  - UX 改善: verify_embedding_meta をモデル DL の前に配置し、不整合時に
    2.3 GB の無駄 DL を回避
  - テスト 28 件パス (3 ignored)、実 DB でミスマッチエラー + 既存モデル継続動作確認
- 次タスク候補: feature 9 (FTS5 ハイブリッド検索 RRF)
- feature 9 実装完了 (status: passing)
  - FTS5 contentless + contentless_delete=1 + trigram tokenizer
  - 実装中の発見: contentless モードでは通常 DELETE が不可 → contentless_delete=1
    (SQLite 3.43+) に切り替えて解決
  - RRF (k=60)、各側 top-N = max(limit*5, 50)。Rust 側で HashMap マージ
  - サニタイズ: 3 文字未満は vec-only、ダブルクォート 2 連化でフレーズ化
  - backfill_fts(): 非破壊的 FTS 埋戻し。rebuild_index 冒頭で自動実行
  - 実 DB (ai_organization/.kb-mcp.db) で 3610 chunks を 4.6s で backfill
    完了、2 回目は 0 件で冪等確認
  - server.rs::search を search_hybrid に差替 (score の意味が distance → RRF)
  - 新規テスト 9 件 (fts_created / insert / delete / upsert / reset / sanitize /
    hybrid_exact / hybrid_fallback / hybrid_ja_trigram / backfill) + 既存 24 件
    = 合計 37 パス (3 ignored)
  - README / CLAUDE.md 更新 (hybrid 動作・RRF score 意味・trigram 下限告知)
- 次タスク候補: feature 10 (Reranking BGE-reranker) または feature 11 以降

## 2026-04-18 (追加セッション)
- ai_organization/kb-mcp.exe を最新バイナリ (feature 7/8/9 込み) に更新済
- BGE-M3 への切替試行 → **ネットワーク接続エラーで中断**
  - `kb-mcp index --model bge-m3 --force` 実行 → TLS "Connection was reset"
    (os error 10054) で HuggingFace から model.onnx の取得失敗
  - curl で huggingface.co への接続も同じく reset → マシン側の NW 問題
    (firewall / AV の TLS inspection 等が疑わしい)
  - DB は無傷 (197 docs / 3610 chunks, bge-small-en-v1.5 のまま)
  - 対応案をユーザへ提示済 (A: NW 変更 / B: HTTPS_PROXY / C: HF_ENDPOINT
    ミラー / D: 手動 DL / E: 切替見送り)
  - `.kb-mcp.db.pre-bge-m3.bak` バックアップ作成済 (Apr 18 07:32)
- evaluator による feature 7/8/9 の品質チェックは未実施 (ユーザ判断待ち)
- BGE-M3 再インデックス完了
  - `hf download BAAI/bge-m3 --include 'onnx/*' ...` で HF CLI (Python TLS)
    経由で model.onnx + model.onnx_data 等を 1:39 で DL 成功
    (fastembed-rs の native_tls 経路は引き続きブロックされる)
  - 回避策: `FASTEMBED_CACHE_DIR=~/.cache/huggingface/hub` 経由で
    fastembed-rs に HF Hub 標準キャッシュを読ませる
  - 再インデックス: 197 docs / 3610 chunks / 約 66 分 (CPU 推論)
  - DB は BGE-M3 (1024 dim) でインデックス済、serve も
    `--model bge-m3` 指定で起動する必要あり
  - kb-mcp リポジトリの .mcp.json を更新:
    - args に "--model", "bge-m3" 追加
    - env.FASTEMBED_CACHE_DIR を ~/.cache/huggingface/hub に設定
    - (.mcp.json は .gitignore で除外、マシンローカル)
  - ai_organization 側には .mcp.json なし (自己参照回避ポリシー)
  - MCP 再接続で BGE-M3 が稼働する状態
- evaluator による feature 7/8/9 品質評価完了: **PASS (26/30, 約 34.7/40)**
  - Product Depth 9, Functionality 9, Code Quality 8
  - ブロッキングなし
  - 非ブロッキング改善候補 6 件:
    1. search_similar と search_vec_candidates の SQL 重複 (dead-code
       化はせず regression ガードとして残すか、chunk_id optional 返却で統合)
    2. filter + RRF 候補数ミスマッチ (filter 選択性高時の件数不足リスク)
    3. ModelChoice::Default は #[derive(Default)] に derive 化可能 (clippy)
    4. insert_chunk docstring の「384-element」記述を動的 dim 記述へ更新
    5. fts_chunks heading の bm25 重み調整余地 (feature 10 と合わせて検討)
    6. BGE-M3 batch size の明示制御 (大規模データ OOM リスク)
- evaluator 指摘 #1-4 を反映 (non-blocking 改善)
  - #1 search_similar を search_vec_candidates への thin wrapper に統合
    (SQL 重複解消、chunk_id 剥ぐだけの委譲)
  - #2 filter over-fetch を search_vec_candidates / search_fts_candidates
    両方に適用 (FILTER_OVERFETCH_FACTOR=10、cap 10_000)
  - #3 ModelChoice::Default を手動実装 → #[derive(Default)] + #[default]
  - #4 insert_chunk docstring の「384-element」を動的 dim 記述へ更新
  - 新規回帰テスト 1 件: test_search_hybrid_overfetches_when_filter_is_selective
  - テスト 38 件 (3 ignored) すべてパス、cargo clippy も軽微な doc 指摘のみ
  - 残 #5 (fts heading 重み) / #6 (BGE-M3 batch) は feature 10 と合わせて検討
- feature 10 仕様化完了 (実装未着手、ユーザー GO/NO-GO 待ち)
  - fastembed-rs v5 の TextRerank API 調査済み
  - RerankerModel enum: BGERerankerBase / BGERerankerV2M3 / JINARerankerV1TurboEn
    / JINARerankerV2BaseMultiligual の 4 variants を確認
  - 選定: BGERerankerV2M3 (~2.3 GB、多言語、日本語 KB 推奨)
    次点: JINARerankerV2BaseMultiligual (~1.2 GB 軽量)
  - デフォルト OFF で opt-in (DL 回避)
  - CLI: --reranker <none|bge-v2-m3|jina-v2-ml|bge-base> enum +
    --rerank-by-default bool。MCP search に rerank: Option<bool> 追加
  - db.search_hybrid_candidates を公開切り出し、search_hybrid は wrapper 化
  - evaluator #5 (fts bm25 heading 重み 2.0/1.0) を同時対応
  - evaluator #6 (BGE-M3 batch size) は分離し feature 10 後に別コミット
  - 5 ステップ実装計画を保留中
- feature 10 実装完了 (status: passing)
  - RerankerChoice enum (None/BgeV2M3/JinaV2Multilingual/BgeBase) 追加
  - Reranker struct: fastembed::TextRerank の薄いラッパ
  - rerank_candidates: (chunk_id, SearchResult) を受け、cross-encoder で
    スコア付け → 降順ソート → top limit 件を返す
  - CLI: --reranker <value> + --rerank-by-default 追加 (Serve のみ)
  - MCP search パラメータに rerank: Option<bool> 追加 (per-query override)
  - KbServer に Mutex<Option<Reranker>> + rerank_by_default フィールド
  - db.search_hybrid_candidates を公開化、search_hybrid は wrapper
  - evaluator #5 同時対応: fts_chunks の bm25 に column weight (heading 2.0,
    content 1.0) を付与。test_fts_bm25_heading_weighted_higher で検証
  - テスト 42 件 (4 ignored) すべてパス
    - 新規: test_search_hybrid_candidates_returns_chunk_ids,
      test_fts_bm25_heading_weighted_higher, test_reranker_choice_values,
      test_reranker_none_returns_none, test_bge_reranker_v2_m3_reorders_ja
      (ignored: 2.3 GB DL 必要)
  - evaluator #6 (BGE-M3 batch size) は feature 10 スコープ外、後続対応
- 次タスク候補: evaluator #6、残 feature (11/15/16/19/20/21/22)
- evaluator 指摘 #6 対応完了
  - ModelChoice::batch_size() を追加 (BGE-small: 256 / BGE-M3: 32)
  - Embedder::embed_texts が self.choice.batch_size() を Some で渡す
  - 計算根拠: batch * 512 * hidden_dim * 4 = BGE-M3@32 で ~67 MB 目安
  - 回帰テスト: test_model_choice_batch_size_is_smaller_for_large_model
    (invariant: BGE-M3 の batch < BGE-small の batch)
  - 既存 DB の再インデックス不要 (embedding 値は batch に依存しない)
  - テスト 43 件 (4 ignored) すべてパス
- ドキュメント全面更新
  - CLAUDE.md: プロジェクト概要を hybrid/rerank 込みに書換、TLS 迂回手順
    (hf download + FASTEMBED_CACHE_DIR) を追記、主要依存一覧を SQLite 3.50+
    / FTS5 / fastembed reranker まで含めて更新、改善候補リストを未実装のみに整理
  - TODO.md: 実装済み表 (feature 7-10 + evaluator #1-6, commit hash 付き) と
    未実装候補を明確に分離、優先度マトリクスを更新
  - docs/design.md: feature 7-10 反映の全面書換。スキーマ (index_meta 順序、
    遅延 vec_chunks、fts_chunks)、検索パイプライン図、ツール定義、決定ログ
    (14 項目) を現行実装に整合
  - docs/plan.md: 冒頭に「全タスク完了・履歴」バナー追加、現行仕様は
    docs/design.md を参照する旨を明記
  - README.md: BGE-M3 + reranker 込みの .mcp.json 設定例、HF 事前 DL 手順を追記
- feature 23 実装完了 (status: passing)
  - バイナリ同居 kb-mcp.toml 設定ファイル対応
  - Cargo.toml に toml = "0.8" 追加
  - src/config.rs 新設: Config 構造体 (全フィールド optional,
    deny_unknown_fields)、load_alongside_binary / load_from /
    apply_cache_dir_env
  - ModelChoice / RerankerChoice に Deserialize + serde(rename) を追加
    (clap::ValueEnum の名前と一致させる)
  - src/main.rs: CLI のすべての kb_path/model/reranker/rerank_by_default を
    Option<T> に変更し、CLI > config > default の優先順位で解決
  - FASTEMBED_CACHE_DIR は env が未設定のときのみ config から注入
    (embedder 初期化より前)
  - kb-mcp.toml.example を同梱、kb-mcp.toml は .gitignore
  - 新規テスト 5 件: missing_file / full / partial / unknown_fields /
    env_precedence
  - テスト 48 件 (4 ignored) すべてパス
  - 実機確認: config のみで --kb-path / --model 省略で動作、CLI override も確認
- feature 23 改善: 相対パスの resolve 基点を設定ファイルのディレクトリに
  - load_from で kb_path / fastembed_cache_dir の相対パスを
    config parent dir 基準に resolve (cwd ではない)
  - MCP 起動時の cwd は呼び出し側プロジェクトに依存するため、設定ファイル
    相対 resolve のほうが直感的で堅牢
  - 新規テスト 2 件: relative_paths_resolve_against_config_dir、
    absolute_paths_are_not_rebased
  - 既存テスト test_parse_full_config を Windows 絶対パス判定に合わせて
    cross-platform 化
  - テスト 50 件 (4 ignored) すべてパス
  - 実機移行は MCP disconnect 待ち (旧バイナリがロック中)
- ai_organization 側デプロイ完了 (MCP 再接続後):
  - 新バイナリ (相対パス fix 込み) を ai_organization/kb-mcp.exe にコピー
  - ai_organization/kb-mcp.toml (kb_path は "./knowledge-base" の相対表記)
  - .mcp.json を args: ["serve"] のみに簡素化
  - 日本語クエリで search 動作確認 (BGE-M3 経由で top-2 が狙った document)
- README.md にユースケース別トレードオフを追記 (commit 8340689):
  - rerank: エージェント対話 / 精度重視 / 混在の推奨表、有効化シグナル
  - model: 比較表 (DL / dim / RAM / build time / 精度)、切替コスト注意
  - .mcp.json: rerank_by_default=false パターン + args: ["serve"] 最小構成
- feature 24 実装完了 (status: passing)
  - src/main.rs に Commands::Search サブコマンド追加 (clap)
  - 引数: <query> (positional) + --kb-path / --model / --reranker / --limit /
    --category / --topic / --format (json|text)
  - SearchFormat enum (Json 既定 / Text LLM-friendly) + print_search_results
  - Database::search_hybrid / Reranker::rerank_candidates を再利用 (新規 DB
    層ロジックなし)
  - kb-mcp.toml の既定値 (model / kb_path 等) は既存経路で自動適用
  - 実機動作確認: 日本語クエリ、text/JSON 両 format、category filter で正常動作
  - テスト 50 件 (4 ignored) パス維持 (main.rs 内のロジックは db/embedder の
    既存テストでカバー)
  - README.md に skill-bin ユースケース例追記、CLAUDE.md に CLI 一覧更新
- evaluator による feature 23/24 + fix 51c31e3 品質評価完了: PASS (25/30)
  - Product Depth 9, Functionality 9, Code Quality 7
  - ブロッキングなし
  - 良かった点: apply_cache_dir_env の unsafe が実際に safe (main.rs で
    Cli::parse 前/Tokio 前の single-threaded 保証)、fix 51c31e3 の設計判断、
    deny_unknown_fields のエラー UX
  - 非ブロッキング改善候補 5 件:
    1. test_relative_paths_resolve_against_config_dir が弱い (tempfile を
       使いながら load_from 経路を通っていない) → tempdir で実 e2e に
    2. Hit (main.rs) と SearchHit (server.rs) の重複定義 →
       From<&SearchResult> transition impl で統合検討
    3. --rerank-by-default の CLI help に default=true 表示なし →
       clap long_help で明示
    4. resolve_relative の Windows UNC / \\?\ プレフィックステスト未カバー
    5. kb-mcp.toml.example と Config のフィールド同期を回帰テスト化
- evaluator 指摘 #1, #2 対応完了
  - #1: test_relative_paths_resolve_against_config_dir を実 e2e に書き換え。
    std::env::temp_dir() 配下に専用サブディレクトリを作り、toml 書込 →
    Config::load_from(&cfg_path) を実行して結果を検証。テスト終了時に
    DirGuard (自前 Drop) でディレクトリごと削除。これで「load_from 内部の
    parent → resolve_relative 経路」を実際にカバー
  - #2: db.rs に pub struct SearchHit + From<SearchResult> を追加し、
    server.rs / main.rs の local 定義 (SearchHit / Hit) を削除して統一。
    MCP `search` ツール出力と CLI `search` サブコマンド出力の shape が
    単一の db::SearchHit で保証される。将来 SearchResult にフィールドを
    追加するときの更新箇所が 1 つに収束
  - テスト 50 件 (4 ignored) 全てパス、実機で CLI search / JSON 出力が
    従来と bit-exact (shape 互換) であることを確認
  - 残 #3-#5 (CLI help の default 明示、UNC テスト、example 同期) は
    小粒で後回し
- evaluator 指摘 #3-#5 対応完了
  - #3: --rerank-by-default の長文 help に "Default: true (when --reranker
    is set). Has no effect while --reranker none." を明記 (clap long_help)
  - #4: test_windows_unc_and_verbatim_paths_not_rebased 追加 (#[cfg(windows)])。
    \\server\share\foo と \\?\C:\verbatim\bar の両方で Path::is_absolute が
    true、resolve_relative で rebase されないことを確認
  - #5: test_toml_example_parses_with_all_keys_uncommented 追加。
    kb-mcp.toml.example を読み、`# key = value` 形式のコメントを剥がして
    Config::toml::from_str でパース。deny_unknown_fields により、example に
    Config から削除されたキーが残ると検知される (削除時の回帰ガード)
  - テスト 52 件 (4 ignored) すべてパス。config テスト 9 件に拡充
- feature 22 実装完了 (YAML frontmatter CRLF 正規化)
  - src/markdown.rs::extract_frontmatter で yaml_str を serde_yaml に渡す
    前に、\r があれば replace("\r\n","\n").replace('\r','\n') で LF 正規化
  - 正規化は \r を含む場合のみ行うので、LF-only の入力にはコスト増なし
  - 新規テスト 1 件: test_crlf_frontmatter_values_have_no_trailing_cr
    (CRLF 入力で title / topic / tags の値に \r が含まれないことを検証)
  - features.json の #22 を passing に
  - テスト 53 件 (4 ignored) 全てパス

## 2026-04-18 Feature 21 — sqlite3_vec_init の transmute 除去

- src/db.rs: `sqlite3_vec_init` の `std::mem::transmute` を廃止
  - sqlite-vec 0.1.x は `fn sqlite3_vec_init()` を引数なしで宣言しているため
    `sqlite3_auto_extension` に渡すには transmute で別 ABI にキャストが必要だった
  - 本プロジェクト側で同シンボルを SQLite 拡張 ABI (db / pzErrMsg / pApi -> c_int)
    で再宣言し、transmute なしで関数ポインタとしてそのまま渡せるようにした
  - `#[link(name = "sqlite_vec0", kind = "static")]` を明示
    (sqlite-vec crate 側の関数を参照しなくなるので dead-code 除去防止 +
     MSVC で __imp_ lookup にならないよう kind=static 指定)
  - unsafe ブロックは `sqlite3_auto_extension` 呼び出しだけに限定
- src/embedder.rs: doc comment の overindented list warning を修正 (clippy)
- src/config.rs: すべての分岐が Some(...) を返す filter_map を map に変更 (clippy)
- verification:
  - cargo clippy --all-targets -- -D warnings がクリーン (warning ゼロ)
  - cargo test: 53 passed / 4 ignored (既存テスト全てパス)
  - src/ から std::mem::transmute が完全除去されたことを grep で確認
- features.json の #21 を passing に

## 2026-04-18 Feature 14 — exclude_headings 設定ファイル化

- src/markdown.rs:
  - `EXCLUDED_HEADINGS` を `pub const DEFAULT_EXCLUDED_HEADINGS` に rename
  - `parse_with_excludes(raw, &[impl AsRef<str>])` を追加
  - 既存 `parse(raw)` は `parse_with_excludes(raw, DEFAULT_EXCLUDED_HEADINGS)` に委譲
  - `chunk_body(body, excludes: &[&str])` に引数を追加 (以前はハードコード)
- src/config.rs:
  - `Config.exclude_headings: Option<Vec<String>>` を追加
  - `is_empty()` の判定に含める
  - 既存の `test_parse_full_config` に exclude_headings も書き足し
  - `test_parse_empty_exclude_headings_overrides_default`: `[]` を None と区別して保持することを検証
- src/indexer.rs:
  - `rebuild_index(... force, exclude_headings: Option<&[String]>)` にシグネチャ拡張
  - None → `markdown::parse` (既定), Some(list) → `parse_with_excludes(list)`
- src/server.rs:
  - `KbServer.exclude_headings: Option<Vec<String>>` を保持
  - `rebuild_index` ツール呼び出しで `self.exclude_headings.as_deref()` を渡す
  - `run_server` にも `exclude_headings` 引数を追加
- src/main.rs:
  - Serve/Index それぞれで `cfg.exclude_headings` を server/indexer に伝搬
- kb-mcp.toml.example:
  - `exclude_headings = ["次の深堀り候補", "参考リンク"]` をコメント例に追加
- 新規テスト:
  - markdown: test_parse_with_empty_excludes_keeps_everything / test_parse_with_custom_excludes
  - config: test_parse_empty_exclude_headings_overrides_default
- verification:
  - cargo test: 56 passed / 4 ignored (53 → 56 で 3 増)
  - cargo clippy --all-targets -- -D warnings: クリーン
- features.json の #14 を passing に

セマンティクス:
  - `None` (未指定) = ["次の深堀り候補"] (後方互換)
  - `Some([])` (明示的な空) = 除外なし
  - `Some([...])` = デフォルトを完全上書き (merge ではなく replace)
  マッチは substring (`heading_text.contains(pattern)`) で、短いパターンで柔軟にヒット。
